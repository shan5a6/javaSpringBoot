apiVersion: v1
kind: Service
metadata:
  name: aidevopsapp
  namespace: aidevopsapp-sit
  labels:
    app: myapp
    costcenter: dvsaidevops
spec:
  type: ClusterIP
  selector:
    app: aidevopsapp
  ports:
  - name: http
    port: 80
    targetPort: 8080
---
