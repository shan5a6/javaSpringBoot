apiVersion: v1
kind: Service
metadata:
  name: application1
  labels:
    app: application1
  namespace: java-dev
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  selector:
    app: application1
  type: LoadBalancer
