trigger:
- none

pool:
  vmImage: ubuntu-latest

steps:
- task: Bash@3
  displayName: "Code Build"
  inputs:
    targetType: 'inline'
    script: |
      #!/bin/bash
      mvn clean package
      ls -l ./target/*.jar

